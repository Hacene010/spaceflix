import styled from 'styled-components';

const SCarrousel = styled.div`
  width: 80vw;
  padding-top: 80px;
  margin: auto;

  img {
    height: 75vh;
    width: 100%;
  }
`;
export default SCarrousel;
